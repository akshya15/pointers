// to arrange element in selection sort as well as one more row to
import java.util.*;
public class Main{
    public static void main(String args[]){
        int []arr={5,6,2,3,1,8,4};
        int []rollno={1,2,3,4,5,6,7};
        int n=arr.length;
        for(int i=0;i<n;i++){
        int minIndex=i;
        for(int j=i+1;j<n;j++){//to find minIndex
            if(arr[j]<arr[minIndex]){
                minIndex=j;
            }
        }//end of it
        int temp=arr[i];
        arr[i]=arr[minIndex];
        arr[minIndex]=temp;
        
        int temp2=rollno[i];
        rollno[i]=rollno[minIndex];
        rollno[minIndex]=temp;
        }
    System.out.println(Arrays.toString(arr));
    System.out.println(Arrays.toString(rollno));
}
}
